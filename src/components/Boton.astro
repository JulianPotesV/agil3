---
export interface Props {
  text: string;
}

const { text } = Astro.props as Props;
---

<button
  type="button"
  class="bg-naranja hover:bg-orange-800 text-white text-lg font-semibold px-8 py-4 rounded-xl transition-colors duration-200"
  aria-label={text}
>
  {text}
</button>
